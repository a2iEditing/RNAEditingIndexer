#! /bin/sh

JAVA_HOME = java

for i in "$@"
do
case $i in
    -j=*|--java_home=*)
    JAVA_HOME="${i#*=}"
    shift # past argument=value
    ;;
esac
done

DEV_ROOT = $PWD
IS_UNIX = true
export DEV_ROOT
export JAVA_HOME
export IS_UNIX


